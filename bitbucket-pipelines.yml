image: ruby:2.4.0

pipelines:
  default:
    - step:
        name: Ruby 2.4
        caches:
          - bundler
        script:
          - bundle install --path ./vendor
          - bundle exec rake spec

definitions:
  caches:
    bundler: ./vendor
